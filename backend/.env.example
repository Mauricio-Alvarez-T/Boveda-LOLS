# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=sgdl
DB_PORT=3306

# JWT
JWT_SECRET=tu_clave_secreta_cambiar_en_produccion
JWT_EXPIRES_IN=8h

# Server
PORT=3000

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
